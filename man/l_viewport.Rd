% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{l_viewport}
\alias{l_viewport}
\title{Create a local layout context for graphical elements}
\usage{
l_viewport(
  plots = list(),
  width = "auto",
  height = "auto",
  units = "px",
  padding = 0,
  margin = 0,
  background = NULL,
  responsive = TRUE,
  safe_area = NULL,
  ...
)
}
\arguments{
\item{plots}{List of ggplots, grid grobs or lplot nodes. A single graphics
object is also accepted. The default creates an empty viewport.}

\item{width, height}{Logical border-box dimensions. At the root these are
reference dimensions for resolution without a device, not forced render
sizes. Use \code{\link[=l_render]{l_render()}} arguments to request explicit drawing dimensions.}

\item{units}{Unit for numeric \code{width} and \code{height}; see \code{\link[=l_length]{l_length()}}. Other
numeric properties remain logical pixels.}

\item{padding, margin}{One to four logical edge lengths, or a named list with
\code{top}, \code{right}, \code{bottom}, \code{left} entries. Padding is inside the box; margin
is outside. Unnamed shorthand follows CSS order: all; vertical/horizontal;
top/horizontal/bottom; or top/right/bottom/left. Missing named sides are zero.}

\item{background}{Optional background fill color. \code{NULL} leaves it unset.}

\item{responsive}{Logical; permits collision-driven shrinking of this node
when minimum dimensions are supplied. Logical units always resolve anew.}

\item{safe_area}{Insets reserved for automatically positioned children,
using the same shorthand as \code{padding}. \code{NULL} means zero insets.}

\item{...}{Additional named node properties accepted by \code{\link[=l_place]{l_place()}}, such
as \code{id}, \code{border}, \code{flow}, \code{gap}, \code{overflow} and dimension constraints.}
}
\value{
An \code{l_viewport}, also inheriting from \code{l_scene} and \code{l_node}.
Construction does not draw; use \code{\link[=l_render]{l_render()}} or \code{\link[grid:grid.draw]{grid::grid.draw()}}.
}
\description{
Build a logical scene containing plots, grobs, extracted elements or nested
viewports. Children are resolved relative to this node's content box when
the scene is measured or drawn, not when it is constructed.
}
\details{
This is a scene-graph context, not a \code{\link[grid:viewport]{grid::viewport()}} and not a table of
cells. Arbitrary positioning is the default. Percentages refer to the local
content area after padding and borders; viewport units refer to the root.
\code{flow = "row"} or \code{"column"} provides sequential placement, while
\code{flow = "stack"} overlays children. Root margins matter only when nested.

Implicit IDs are assigned from tree paths such as \code{root/1}. Explicit IDs must
be unique within the scene. Objects are copied logically; no active plot or
global registry is consulted. Automatic sizing does not wrap text or infer
a bounding box for arbitrary grid trees. See \code{\link[=l_place]{l_place()}} for collision and
clipping rules and \code{\link[=l_template]{l_template()}} for composition inside a single grob.
}
\examples{
scene <- l_viewport(list(
  l_place(l_rect(fill = "#95CEC0", col = NA),
    left = "10\%", top = "10\%", width = "80\%", height = "80\%"
  ),
  l_place(l_text("Local context"), x = "50\%", y = "50\%", anchor = "center")
), width = 400, height = 200, padding = 10, background = "white")
l_resolve(scene, width = 800, height = 400)$root$children[[1]]$box
l_render(scene, width = 400, height = 200)
}
\seealso{
\code{\link[=l_place]{l_place()}}, \code{\link[=l_join]{l_join()}}, \code{\link[=l_resolve]{l_resolve()}}, \code{\link[=l_template]{l_template()}}
}
