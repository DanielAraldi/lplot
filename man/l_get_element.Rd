% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{l_get_element}
\alias{l_get_element}
\title{Extract and normalize a semantic graphical element}
\usage{
l_get_element(
  plot,
  type,
  width = "auto",
  height = "auto",
  style = NULL,
  position = NULL,
  responsive = TRUE,
  collision = "none",
  priority = 0,
  registry = l_registry(),
  which = NULL,
  missing = c("error", "null"),
  ...
)
}
\arguments{
\item{plot}{Explicit source ggplot, grid grob or object accepted by a custom
registry adapter. Required; no implicit current or active plot is used.}

\item{type}{Single character semantic type registered in \code{registry}. See
\code{\link[=l_registry]{l_registry()}} for built-in types and direct-grob categories.}

\item{width, height}{Logical border-box dimensions accepted by \code{\link[=l_length]{l_length()}}.
\code{"auto"} uses intrinsic content or available space, depending on the type.}

\item{style}{Optional named list of element-specific overrides passed to
\code{\link[=l_style]{l_style()}}, such as \code{color} and \code{font_size}.}

\item{position}{Optional named list of node properties applied after the
initial properties, before style overrides; see \code{\link[=l_place]{l_place()}}.}

\item{responsive}{Logical permission for collision-driven shrinking.
This does not disable resolution of logical units on subsequent draws.}

\item{collision}{Collision policy: \code{"none"}, \code{"avoid"}, \code{"shrink"} or
\code{"avoid-and-shrink"}. See \code{\link[=l_place]{l_place()}} for movement and shrinking conditions.}

\item{priority}{Finite numeric priority for collision placement. Larger
values are handled first.}

\item{registry}{Explicit \code{l_registry}, normally from \code{\link[=l_registry]{l_registry()}} or
\code{\link[=l_register_element]{l_register_element()}}.}

\item{which}{Optional positive integer selecting one nonempty component
when several match. With \code{NULL}, native extraction preserves the matched
components' gtable arrangement. Custom extractors must handle this argument
if supplied.}

\item{missing}{\code{"error"} raises an error for an absent component; \code{"null"}
returns \code{NULL} for permitted absence. It does not hide unsupported sources,
unknown extractors or arbitrary callback errors.}

\item{...}{Additional named node properties accepted by \code{\link[=l_place]{l_place()}}, such
as \code{id}, \code{metadata}, \code{padding} and dimension constraints. These arguments
are not forwarded to the extraction callback.}
}
\value{
An \code{l_element} inheriting from \code{l_node}, or \code{NULL} when permitted
absence is requested. The node retains its source, adapter and declarations.
}
\description{
Extract a component from an explicit ggplot source or wrap a supplied grob
as a semantic element. The source and unrelated components are preserved;
the returned element can be measured, restyled and positioned independently.
}
\details{
For a ggplot source, the effective global and plot-specific theme is
captured at extraction. Later changes to the global ggplot theme do not
silently restyle the element. Native content may be rebuilt for the final
context to apply responsive typography while preserving inherited styling.

Extraction is separate from removal. To avoid drawing a title or legend
twice, use \code{\link[=l_without]{l_without()}} on the base plot when composing extracted elements.
Direct grobs are accepted as supplied; choosing \code{"north_arrow"} or
\code{"scale_bar"} does not perform geographic calculations.
}
\section{Conditions}{

Missing native components raise \code{lplot_missing_element}; unknown types
raise \code{lplot_missing_extractor}; rejected sources raise
\code{lplot_unsupported_source}; non-grob extraction results raise
\code{lplot_invalid_extractor}. These inherit from \code{lplot_error}.
}

\examples{
plot <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  ggplot2::labs(title = "Efficiency")
title <- l_get_element(plot, "title",
  style = list(color = "#194E70", font_size = "clamp(10pt, 2vmin, 18pt)")
)
l_measure(title, viewport = list(width = 600, height = 400))
l_get_element(ggplot2::ggplot(), "title", missing = "null")
credits <- l_get_element(l_text("Source: survey"), "credits",
  metadata = list(source = "survey")
)
print(credits)
}
\seealso{
\code{\link[=l_without]{l_without()}}, \code{\link[=l_style]{l_style()}}, \code{\link[=l_register_element]{l_register_element()}}, \code{\link[=l_place]{l_place()}}
}
