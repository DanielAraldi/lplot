% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{l_without}
\alias{l_without}
\title{Remove semantic components from a copy of a ggplot}
\usage{
l_without(plot, type)
}
\arguments{
\item{plot}{A ggplot object. Grobs and lplot scene nodes are not accepted.}

\item{type}{Character vector of native semantic types to remove. Supported
types are \code{title}, \code{subtitle}, \code{caption}, \code{tag}, \code{legend}, \code{x_axis},
\code{y_axis}, \code{x_axis_title}, \code{y_axis_title}, \code{strip}, \code{plot_background} and
\code{panel_background}. Removing \code{panel} is deliberately unsupported.}
}
\value{
A modified ggplot value. The supplied \code{plot} is unchanged.
}
\description{
Hide selected labels, legends, axes or backgrounds without changing the
original plot. Use this when separately extracted components are being
composed elsewhere in an lplot scene.
}
\details{
Text labels are cleared with \code{\link[ggplot2:labs]{ggplot2::labs()}}, legends are hidden through
the theme, and other components are replaced with blank theme elements.
Removing an axis hides its text, ticks and line; its title is a separate
type. Removing strips blanks strip text and backgrounds. This is not an
operation on the plot's data, scales or geographic extent.

Unknown types raise an \code{lplot_error}. Unsupported source classes raise
\code{lplot_unsupported_source}. No extractor registry is consulted.
}
\examples{
plot <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  ggplot2::labs(title = "Efficiency")
title <- l_get_element(plot, "title")
base <- l_without(plot, c("title", "x_axis_title"))
l_get_element(base, "title", missing = "null")
l_render(l_viewport(list(
  l_place(base, left = 0, right = 0, top = 40, bottom = 0),
  l_place(title, left = 20, top = 8)
)), width = 400, height = 300)
}
\seealso{
\code{\link[=l_get_element]{l_get_element()}}, \code{\link[=l_viewport]{l_viewport()}}, \code{\link[=l_place]{l_place()}}
}
