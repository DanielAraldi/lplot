% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-grid.R
\name{grid.draw.l_node}
\alias{grid.draw.l_node}
\title{Draw an lplot node through the grid generic}
\usage{
\method{grid.draw}{l_node}(x, recording = TRUE)
}
\arguments{
\item{x}{An \code{l_node} or subclass, such as an \code{l_viewport} or \code{l_element}.}

\item{recording}{Logical; passed to grid to control whether this draw is
recorded on the grid display list.}
}
\value{
\code{x}, invisibly and unchanged, after drawing on the current device.
}
\description{
Provide \code{\link[grid:grid.draw]{grid::grid.draw()}} interoperability for lplot scenes and elements.
The node is wrapped as a deferred grob and resolved in the current viewport.
}
\details{
This method does not begin a new page. Call \code{\link[grid:grid.newpage]{grid::grid.newpage()}} explicitly
when needed. It uses the default options of \code{\link[=l_as_grob]{l_as_grob()}}. For explicit
dimensions use \code{\link[=l_render]{l_render()}}; for grid drawing with diagnostics, draw
\code{l_as_grob(x, debug = TRUE)} instead. Device replay can resolve the deferred
content again at the new viewport size.
}
\examples{
scene <- l_viewport(list(l_place(l_text("Grid interoperability"),
  left = 12, top = 12
)))
grid::grid.newpage()
returned <- grid::grid.draw(scene)
identical(returned, scene)
}
\seealso{
\code{\link[=l_render]{l_render()}}, \code{\link[=l_as_grob]{l_as_grob()}}, \code{\link[grid:grid.draw]{grid::grid.draw()}}
}
