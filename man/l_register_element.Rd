% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extractors.R
\name{l_register_element}
\alias{l_register_element}
\title{Register a custom semantic element adapter}
\usage{
l_register_element(
  type,
  can_extract = NULL,
  extract = NULL,
  measure = NULL,
  style = NULL,
  registry = l_registry(),
  extractor = NULL,
  measurer = NULL
)
}
\arguments{
\item{type}{Single nonempty character identifier for the semantic element.
An existing entry with this name is replaced in the returned registry.}

\item{can_extract}{Optional predicate called with the source as its first
argument. It must return \code{TRUE} to accept the source. The default accepts
any source and leaves validation to the extractor.}

\item{extract}{Required extraction function, unless \code{extractor} is supplied.
Called with the source as its first argument and, when requested, a named
\code{which} argument. It must return a grid grob. Accepting \code{...} is useful for
forward-compatible adapters.}

\item{measure}{Optional \verb{function(element, context)} returning named \code{width}
and \code{height} values in logical pixels. \code{element$content} contains the
prepared, styled grob. With \code{NULL}, grid intrinsic measurement is used.}

\item{style}{Optional \verb{function(element, style, context)} returning a grob.
It receives resolved style lengths in logical pixels and is responsible
for applying custom styles and validating any extra property names.}

\item{registry}{An \code{l_registry} from \code{\link[=l_registry]{l_registry()}} or an earlier call to
this function. The original value is not modified.}

\item{extractor, measurer}{Aliases for \code{extract} and \code{measure}. The canonical
arguments take precedence when non-\code{NULL}.}
}
\value{
An updated \code{l_registry}. Always retain the return value and pass it
to \code{\link[=l_get_element]{l_get_element()}}; the default registry is never changed globally.
}
\description{
Add or replace an extraction adapter in a copy of an explicit registry.
This extends the accepted source types without modifying the layout engine
or installing global callbacks.
}
\details{
Callback contexts contain \code{width}, \code{height}, \code{root_width}, \code{root_height}
and \code{dpi}. Widths and heights are logical pixels. Graphical preparation may
occur more than once as measurement and rendering use different contexts;
callbacks should be deterministic and avoid external side effects.

A custom style callback allows additional style names through \code{\link[=l_style]{l_style()}}.
Logical lengths resolved by lplot still use its units, not raw grid font
points. Returning anything other than a grob from extraction is rejected.
\code{NULL} from extraction is accepted only when \code{\link[=l_get_element]{l_get_element()}} is called
with \code{missing = "null"}.
}
\examples{
registry <- l_register_element(
  "badge",
  can_extract = is.character,
  extract = function(plot, ...) l_text(plot, fontsize = 10),
  measure = function(element, context) c(width = 100, height = 24)
)
badge <- l_get_element("Survey area", "badge", registry = registry)
l_measure(badge)
"badge" \%in\% names(l_registry())
}
\seealso{
\code{\link[=l_registry]{l_registry()}}, \code{\link[=l_get_element]{l_get_element()}}, \code{\link[=l_measure]{l_measure()}}, \code{\link[=l_style]{l_style()}}
}
