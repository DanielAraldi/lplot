% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{l_place}
\alias{l_place}
\title{Position and constrain a graphics object in a scene}
\usage{
l_place(
  object,
  x = NULL,
  y = NULL,
  width = "auto",
  height = "auto",
  top = NULL,
  right = NULL,
  bottom = NULL,
  left = NULL,
  anchor = "top-left",
  z_index = 0,
  ...
)
}
\arguments{
\item{object}{A ggplot, grid grob, extracted \code{l_element}, or existing lplot
scene node. A ggplot's effective theme is captured when it becomes a node.}

\item{x, y}{Optional anchor coordinates measured from the top-left of the
containing content box. Positive \code{y} points down. Values use \code{\link[=l_length]{l_length()}}.}

\item{width, height}{Logical border-box dimensions accepted by \code{\link[=l_length]{l_length()}}.
\code{"auto"} uses content or available space, depending on the node type.}

\item{top, right, bottom, left}{Optional edge insets. Opposing insets determine
an automatic dimension. Do not combine \code{x} with horizontal insets or \code{y}
with vertical insets; conflicting constraints raise an error.}

\item{anchor}{Anchor point used for coordinates or implicit docking. One of
\code{"top-left"}, \code{"top-center"}, \code{"top-right"}, \code{"center-left"}, \code{"center"},
\code{"center-right"}, \code{"bottom-left"}, \code{"bottom-center"}, \code{"bottom-right"}.}

\item{z_index}{Finite numeric drawing order. Higher values draw later;
original child order breaks ties.}

\item{...}{Uniquely named additional node properties, described below.
Unknown properties raise an \code{lplot_error}.}
}
\value{
A positioned \code{l_node}, preserving subclasses when \code{object} was
already a scene node. Child-local declarations are retained.
}
\description{
Wrap a ggplot or grob as a scene node, or return an existing node with updated
logical placement properties. No drawing or device-dependent conversion
occurs, and the input object is not modified.
}
\details{
Omitted placement arguments preserve an existing node's declarations.
Explicit \code{NULL} clears an optional coordinate or constraint. Bare numbers
are logical pixels, percentages use the parent's content box, and viewport
units use the root. Padding and borders are inside the box; margins are
outside. See \code{\link[=l_length]{l_length()}} for units and \code{\link[=l_viewport]{l_viewport()}} for edge shorthand.
}
\section{Additional properties}{

\itemize{
\item \code{min_width}, \code{max_width}, \code{min_height}, \code{max_height}: optional logical
dimension bounds; \code{aspect_ratio}: positive width-to-height ratio.
\item \code{id}: optional nonempty node identifier, unique within a scene.
\item \code{margin}, \code{padding}, \code{safe_area}: edge lengths; the safe area constrains
automatic child positioning.
\item \code{background}: fill color or \code{NULL}; \code{border}: a color or a list with
\code{color}, \code{width} and \code{line_type} entries.
\item \code{overflow}: \code{"visible"} (default), \code{"hidden"} or \code{"inherit"}. Clipping
does not repair geometry or automatically suppress overflow warnings.
\item \code{flow}: \code{"absolute"} (default), \code{"row"}, \code{"column"} or \code{"stack"};
\code{gap}: logical spacing for row/column flow.
\item \code{collision}: \code{"none"} (default), \code{"avoid"}, \code{"shrink"} or
\code{"avoid-and-shrink"}; \code{priority}: finite numeric placement priority;
\code{allow_move}: whether explicitly positioned nodes may be moved;
\code{candidates}: optional character vector of alternative anchors.
\item \code{responsive}: logical permission for collision-driven shrinking, not a
switch that freezes percentage or viewport-relative units.
\item \code{metadata}: application-specific metadata. \code{metadata$obstacle} overrides
whether the node is treated as an obstacle during collision resolution.
}
}

\section{Collision behavior}{

Collision handling is opt-in and based on sibling boxes, not painted map
features. Higher priorities are processed first. Explicit positions are
protected unless \code{allow_move = TRUE}. Shrinking requires \code{responsive = TRUE}
and a minimum dimension, with a 10 percent scale safety floor. Whole plots
and panel/background elements are normally excluded as obstacles. Failed
placement preserves a fallback and emits \code{lplot_collision}; an overflowing
box emits \code{lplot_overflow}. Inspect \code{\link[=l_resolve]{l_resolve()}} results for diagnostics.
}

\examples{
label <- l_place(l_text("Survey area"),
  x = "50\%", top = 10,
  anchor = "top-center", id = "label"
)
scene <- l_viewport(list(label), width = 300, height = 100)
l_resolve(scene)$root$children[[1]]$box
moved <- l_place(label, x = NULL, left = 20)
l_resolve(l_viewport(list(moved)), width = 300, height = 100)
}
\seealso{
\code{\link[=l_viewport]{l_viewport()}}, \code{\link[=l_join]{l_join()}}, \code{\link[=l_length]{l_length()}}, \code{\link[=l_render]{l_render()}}
}
