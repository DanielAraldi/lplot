% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates.R
\name{l_unit}
\alias{l_unit}
\title{Create native grid units}
\usage{
l_unit(x, units, data = NULL)
}
\arguments{
\item{x}{Numeric vector of values interpreted in \code{units}.}

\item{units}{Character vector of grid unit names, recycled according to
\code{\link[grid:unit]{grid::unit()}} rules. Examples include \code{"npc"}, \code{"native"}, \code{"mm"}, \code{"cm"},
\code{"inches"}, \code{"points"}, \code{"lines"}, \code{"char"}, \code{"null"}, \code{"strwidth"},
\code{"strheight"}, \code{"grobwidth"} and \code{"grobheight"}. See \code{\link[grid:unit]{grid::unit()}} for the
complete list and supported aliases.}

\item{data}{Optional auxiliary data required by some units, passed unchanged
to \code{\link[grid:unit]{grid::unit()}}. Supply text or an expression for string-based units, a
grob or \code{\link[grid:gPath]{grid::gPath()}} for grob-based units, or a list for multiple values,
following grid's recycling rules. Defaults to \code{NULL}.}
}
\value{
A native grid \code{unit} object, identical to the result of
\code{grid::unit(x, units, data = data)}. It is not an \code{l_length} or a grob.
}
\description{
Construct a unit object by delegating directly to \code{\link[grid:unit]{grid::unit()}}. The
arguments, validation, vectorization and return value follow grid's native
behavior. No lplot-specific units or conversions are added.
}
\details{
This function creates a unit specification, not a converted numeric length.
Relative units and font or grob measurements are evaluated by grid in the
applicable viewport and graphics-device context. Use \code{\link[grid:grid.convert]{grid::convertUnit()}},
\code{\link[grid:grid.convert]{grid::convertWidth()}} or \code{\link[grid:grid.convert]{grid::convertHeight()}} for explicit conversions.
Construction does not draw or open a graphics device.

Unit objects work with \code{\link[=l_rect]{l_rect()}}, \code{\link[=l_text]{l_text()}}, native grid grobs and
viewports, including the children of \code{\link[=l_template]{l_template()}}. Native grid arithmetic,
indexing and \code{\link[grid:unit.c]{grid::unit.c()}} remain available. With \code{"npc"}, \verb{(0, 0)} is
the bottom-left corner and \verb{(1, 1)} is the top-right corner of the viewport.
The \code{"null"} unit is meaningful for relative sizing in \code{\link[grid:grid.layout]{grid::grid.layout()}}.

These are not lplot layout lengths. Use \code{\link[=l_length]{l_length()}} or layout strings for
\code{\link[=l_place]{l_place()}} and \code{\link[=l_viewport]{l_viewport()}} constraints. For example, \code{l_unit(0.5, "npc")}
describes half a native grid viewport, whereas \code{l_length("50\%")} describes
half the relevant lplot parent dimension. CSS-like \code{"px"}, \code{"\%"}, \code{"vw"},
\code{"auto"} and \code{"clamp(...)"} are not grid unit names. Invalid input raises
grid's own errors, not lplot-specific conditions.
}
\examples{
spacing <- l_unit(5, "mm")
grid::is.unit(spacing)
l_unit(c(0.25, 0.75), "npc")
l_unit(c(1, 5), c("cm", "mm"))
l_unit(1, "strwidth", data = "Survey area")
outline <- l_rect(
  width = l_unit(20, "mm"), height = l_unit(10, "mm"),
  fill = NA, col = "black"
)
grid::is.grob(outline)
l_unit(1, "grobwidth", data = outline)
l_unit(1, "npc") - spacing
}
\seealso{
\code{\link[grid:unit]{grid::unit()}}, \code{\link[grid:grid.convert]{grid::convertUnit()}}, \code{\link[grid:unit.c]{grid::unit.c()}},
\code{\link[=l_length]{l_length()}}, \code{\link[=l_rect]{l_rect()}}, \code{\link[=l_text]{l_text()}}, \code{\link[=l_template]{l_template()}}
}
