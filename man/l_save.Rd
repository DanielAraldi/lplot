% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{l_save}
\alias{l_save}
\title{Export a plot or scene to an image file}
\usage{
l_save(
  plot,
  type = "png",
  dir = ".",
  filename = "plot",
  width = 800,
  height = 600,
  dpi = 96,
  background = "white",
  quality = 90,
  overwrite = FALSE
)
}
\arguments{
\item{plot}{An explicit ggplot, grid grob, \code{l_element} or \code{l_viewport}/scene.
A filename, screenshot or resolved \code{l_layout} is not an accepted source.}

\item{type}{Single case-insensitive format: \code{"jpg"}, \code{"jpeg"}, \code{"png"},
\code{"svg"} or \code{"webp"}. JPG and JPEG use the same encoder.}

\item{dir}{Destination directory, created recursively when necessary.
Defaults to the working directory; do not include the filename here.}

\item{filename}{Basename without directory components. The selected extension
is appended if absent. A supplied extension must match \code{type}, with JPG and
JPEG treated as interchangeable.}

\item{width, height}{Positive numeric dimensions in logical pixels. Defaults
are 800 by 600, independent of the scene's root reference dimensions.}

\item{dpi}{Positive raster density. Raster output is
\code{round(width * dpi / 96)} by \code{round(height * dpi / 96)} pixels. SVG dimensions
are \code{width / 96} by \code{height / 96} inches, independent of DPI.}

\item{background}{Single valid device background color. PNG, SVG and WebP
allow \code{"transparent"}; JPEG requires an opaque color. Opaque scene
backgrounds still cover the device background.}

\item{quality}{Number from zero to 100 for lossy JPEG/WebP encoding. It is
validated but has no effect on PNG or SVG.}

\item{overwrite}{Logical; whether an existing file may be replaced. The
default \code{FALSE} protects existing output.}
}
\value{
The normalized absolute path of the exported file, invisibly.
The output file and, if necessary, its parent directory are created as
side effects.
}
\description{
Render explicit graphical content to JPEG, PNG, SVG or WebP using an
independent graphics device. Export dimensions do not depend on the Plots
pane or on the size of the current device.
}
\section{Optional codecs}{

PNG and JPEG require \strong{ragg}, SVG requires \strong{svglite}, and WebP requires
\strong{ragg} and \strong{webp}. Missing packages raise \code{lplot_missing_dependency} with
installation guidance; no packages are installed automatically. SVG preserves
vector content, but source raster layers remain embedded raster images.
}

\section{Device and file safety}{

Export opens its own device, draws with \code{\link[=l_render]{l_render()}}, closes that device and
restores the caller's device, including after rendering errors. It does not
depend on an implicit last plot and does not mutate scene declarations.

A temporary file is written in the destination directory and renamed only
after successful rendering and encoding. A rendering failure leaves an
existing destination unchanged. Temporary files are removed; a newly created
directory may remain after an error. Concurrent writers to the same path
are not coordinated.
}

\section{Limits and conditions}{

Raster output must have at least one pixel per axis and at most 100 million
pixels in total. WebP additionally limits each axis to 16383 pixels. Higher
DPI increases raster density without changing logical layout size. Font
metrics can differ between graphics devices.

Invalid arguments raise \code{lplot_error}; an existing destination raises
\code{lplot_file_exists}; directory/publication failures raise \code{lplot_export}.
Layout warnings remain visible and normally do not prevent saving, whereas
invalid constraints still fail. PDF is not supported by \code{type}; use a PDF
device with \code{\link[=l_render]{l_render()}} when that format is required.
}

\examples{
scene <- l_viewport(list(l_place(l_text("Map export"), left = 12, top = 12)))
if (requireNamespace("ragg", quietly = TRUE)) {
  file <- l_save(scene,
    type = "png", dir = tempdir(),
    filename = basename(tempfile("lplot-")), width = 400, height = 200,
    dpi = 144
  )
  stopifnot(file.exists(file))
  unlink(file)
}
if (requireNamespace("svglite", quietly = TRUE)) {
  file <- l_save(scene,
    type = "svg", dir = tempdir(),
    filename = basename(tempfile("lplot-")), width = 400, height = 200,
    background = "transparent"
  )
  stopifnot(file.exists(file))
  unlink(file)
}
}
\seealso{
\code{\link[=l_render]{l_render()}}, \code{\link[=l_viewport]{l_viewport()}}, \code{\link[=l_resolve]{l_resolve()}}
}
