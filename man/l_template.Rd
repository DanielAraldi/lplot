% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates.R
\name{l_template}
\alias{l_template}
\title{Compose reusable graphical templates}
\usage{
l_template(
  ...,
  children = NULL,
  name = NULL,
  gp = NULL,
  vp = NULL,
  childrenvp = NULL,
  cl = NULL
)
}
\arguments{
\item{...}{Child grobs in drawing order. Later children are drawn on top of
earlier children. These arguments are content, not graphical parameters.}

\item{children}{Optional list or \code{\link[grid:grid.grob]{grid::gList()}} of grobs appended after the
children supplied in \code{...}. An empty template is allowed. Each supplied
child must be a grob; strings, \code{NULL}, ggplots and lplot nodes are not grobs.}

\item{name}{Optional character name for the grid grob. This is distinct from
a scene node's \code{id} in \code{\link[=l_place]{l_place()}}.}

\item{gp}{\code{NULL}, a \code{\link[grid:gpar]{grid::gpar()}} object, or a uniquely named list of grid
graphical parameters. Unspecified child properties inherit from this group.}

\item{vp}{Optional grid viewport, viewport name or viewport path. A viewport
controls local coordinates, dimensions, justification, scales and clipping.}

\item{childrenvp}{Optional native grid viewport tree for the children, as
accepted by \code{\link[grid:grid.grob]{grid::grobTree()}}.}

\item{cl}{Optional character vector of additional classes for the gTree.
Intended for grid extensions; normally leave this as \code{NULL}.}
}
\value{
A native grid \code{gTree} inheriting from \code{grob}. It can be passed to
\code{\link[grid:grid.draw]{grid::grid.draw()}}, \code{\link[=l_place]{l_place()}}, \code{\link[=l_get_element]{l_get_element()}} or \code{\link[=l_save]{l_save()}}.
}
\description{
Combine native grid grobs into a single graphical object without drawing it.
Templates can contain rectangles, text, nested templates and other grid
primitives, and can be placed as one element in an lplot scene.
}
\details{
Templates preserve native grid drawing order and inheritance. Explicit child
settings such as \code{col} take precedence over inherited settings; parameters
such as \code{alpha}, \code{cex} and \code{lex} can be multiplicative across the hierarchy.
Neither the input grobs nor their graphical parameters are modified.

Internal coordinates follow grid conventions: with \code{npc} units, \verb{(0, 0)} is
the bottom-left corner and \verb{(1, 1)} is the top-right corner. Use \code{\link[=l_place]{l_place()}}
for outer positioning in lplot's top-left coordinate system. Templates have
no automatic child bounding-box measurement: supply explicit layout sizes
or opposing insets. Enlarging the box does not scale fixed font sizes.

A template is graphical content, not a layout container. Use \code{\link[=l_viewport]{l_viewport()}}
when children need independent constraints, margins or collision policies.
Wrap a template in \code{\link[=l_get_element]{l_get_element()}} to attach semantic metadata and styles.
These constructors do not draw, open devices or register global state.
}
\examples{
badge <- l_template(
  l_rect(fill = "white", col = "#203C43"),
  l_text("N", fontsize = 12, fontface = "bold"),
  gp = list(col = "#203C43")
)
scene <- l_viewport(list(
  l_place(badge, right = 12, top = 12, width = 40, height = 50)
), width = 240, height = 120)
l_render(scene, width = 240, height = 120)
nested <- l_template(children = list(badge, l_text("Survey", y = 0.1)))
grid::is.grob(nested)
}
\seealso{
\code{\link[=l_rect]{l_rect()}}, \code{\link[=l_text]{l_text()}}, \code{\link[grid:grid.grob]{grid::grobTree()}}, \code{\link[grid:viewport]{grid::viewport()}}
}
