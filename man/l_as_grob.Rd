% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-grid.R
\name{l_as_grob}
\alias{l_as_grob}
\title{Wrap a scene in a grid grob with deferred layout}
\usage{
l_as_grob(object, dpi = 96, debug = FALSE)
}
\arguments{
\item{object}{An lplot scene, viewport, element, ggplot or grid grob.}

\item{dpi}{Positive output-density metadata; logical pixels remain 1/96 inch.}

\item{debug}{Logical; whether drawing should add layout diagnostic overlays
for boxes, content areas, margins, intrinsic bounds, anchors and candidates.}
}
\value{
A \code{l_scene_grob}, also a grid \code{gTree} and \code{grob}, storing the logical
scene and drawing options. Its children are prepared at drawing time.
}
\description{
Create a grid-compatible wrapper that retains a logical scene and resolves
it each time grid draws it. Construction does not draw or measure the scene.
}
\details{
This is an interoperability wrapper, not a frozen image or precomputed
layout. \code{\link[grid:grid.draw]{grid::grid.draw()}} invokes its grid \code{makeContent} method, which
resolves dimensions against the current viewport on each draw. Use a grid
viewport to control its space when embedding it in another grid composition.

The wrapper does not implement custom \code{widthDetails} or \code{heightDetails}
methods. External systems that request intrinsic dimensions may therefore
need an explicit viewport. Use \code{\link[=l_measure]{l_measure()}} or \code{\link[=l_resolve]{l_resolve()}} for lplot size
inspection; do not assume automatic sizing in every third-party grob layout.
}
\examples{
scene <- l_viewport(list(l_place(l_text("Deferred layout"), left = 10, top = 10)))
grob <- l_as_grob(scene)
grid::is.grob(grob)
grid::grid.newpage()
grid::grid.draw(grob)
}
\seealso{
\code{\link[=l_render]{l_render()}}, \code{\link[grid:grid.draw]{grid::grid.draw()}}, \code{\link[grid:makeContent]{grid::makeContent()}}
}
