% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{l_join}
\alias{l_join}
\title{Join graphical scenes while preserving local coordinates}
\usage{
l_join(
  plots,
  width = "auto",
  height = "auto",
  position = NULL,
  gap = 0,
  background = NULL,
  ...
)
}
\arguments{
\item{plots}{List of ggplots, grid grobs or lplot nodes. A single graphics
object is also accepted. The default creates an empty viewport.}

\item{width, height}{Logical border-box dimensions. At the root these are
reference dimensions for resolution without a device, not forced render
sizes. Use \code{\link[=l_render]{l_render()}} arguments to request explicit drawing dimensions.}

\item{position}{Optional uniquely named list of \code{\link[=l_place]{l_place()}} node properties
applied to the joined parent after construction, not to its children.}

\item{gap}{Logical spacing between children in row or column flow. Ignored
by absolute positioning, the default flow.}

\item{background}{Optional background fill color. \code{NULL} leaves it unset.}

\item{...}{Additional arguments forwarded to \code{\link[=l_viewport]{l_viewport()}}, such as
\code{padding}, \code{flow}, \code{border}, \code{overflow} and other node properties.}
}
\value{
An \code{l_viewport}, also inheriting from \code{l_scene} and \code{l_node}.
The input scenes are not modified.
}
\description{
Create a parent viewport around existing graphics objects or scenes without
flattening their child declarations. Joining alone does not imply a grid or
automatic side-by-side placement.
}
\details{
Position children with \code{\link[=l_place]{l_place()}} before joining, or request \code{flow = "row"}
or \code{flow = "column"}. Each nested scene keeps its own local coordinates and
is resolved in the box allocated by the parent. Moving the joined object
later does not rewrite those child declarations.
}
\examples{
first <- l_viewport(list(l_place(l_text("First"), left = 8, top = 8)),
  background = "#DCEFE8"
)
second <- l_viewport(list(l_place(l_text("Second"), left = 8, top = 8)),
  background = "#EDF3F5"
)
joined <- l_join(list(
  l_place(first, left = "0\%", width = "48\%", height = "100\%"),
  l_place(second, left = "52\%", width = "48\%", height = "100\%")
), width = 400, height = 160)
l_render(joined, width = 400, height = 160)
}
\seealso{
\code{\link[=l_viewport]{l_viewport()}}, \code{\link[=l_place]{l_place()}}, \code{\link[=l_resolve]{l_resolve()}}
}
