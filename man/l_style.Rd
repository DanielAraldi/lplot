% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{l_style}
\alias{l_style}
\title{Override an element's style without modifying its source}
\usage{
l_style(object, ...)
}
\arguments{
\item{object}{An extracted element, grid grob, ggplot or lplot scene node.
Non-node objects are first normalized as scene nodes.}

\item{...}{Uniquely named style properties. Supported names depend on the
element type; unknown or inapplicable names raise an \code{lplot_error}.}
}
\value{
A styled \code{l_node}, retaining any element or viewport subclasses.
With no overrides, returns the normalized node unchanged.
}
\description{
Return a scene node with explicit style overrides. Unspecified properties
retain their source semantics, and responsive lengths are evaluated in the
actual measurement or drawing context.
}
\details{
\code{font_size}, \code{line_width}, \code{legend.key_width} and \code{legend.key_height} accept
logical lengths including \code{clamp()} expressions, but not \code{auto}. A numeric
\code{font_size} is in logical pixels; use \code{"12pt"} for twelve points. This
differs from the raw grid \code{fontsize} argument of \code{\link[=l_text]{l_text()}}. Edge and border
properties follow \code{\link[=l_place]{l_place()}} and \code{\link[=l_viewport]{l_viewport()}} conventions.

Native ggplot overrides target only the selected semantic theme component.
Axis typography targets axis text, not tick geometry. For direct grobs,
applicable graphical overrides are applied recursively to descendants, so
this is an explicit restyling operation rather than mere group inheritance.
Adapter-defined style callbacks may support additional names; see
\code{\link[=l_register_element]{l_register_element()}}.

Repeated calls merge explicit overrides without mutating the source or
earlier nodes. Styling does not wrap text or guarantee that it fits a box.
Use \code{\link[=l_measure]{l_measure()}} and \code{\link[=l_resolve]{l_resolve()}} to inspect the resulting geometry.
}
\section{Supported styles}{

\itemize{
\item Text elements: \code{color} (or \code{colour}, but not both), \code{font_family},
\code{font_size}, \code{font_face} and \code{line_height}.
\item Generic grobs: the text properties plus \code{fill}, \code{line_width}, \code{line_type}.
\item Plot/panel background elements: \code{fill}, \code{color}, \code{line_width}, \code{line_type}.
\item Native legends additionally accept \code{legend.direction} (\code{"horizontal"}
or \code{"vertical"}), \code{legend.key_width} and \code{legend.key_height}.
\item Elements accept presentation properties \code{background}, \code{border}, \code{padding},
\code{margin}, \code{alpha} and \code{opacity}. Opacity values must be between zero and one;
\code{opacity} takes precedence over \code{alpha} when both are present.
\item Whole plots and viewports accept only \code{background}, \code{border}, \code{padding}
and \code{margin}, not typography or alpha overrides.
}
}

\examples{
original <- l_get_element(l_text("Survey area", fontsize = 10), "annotation")
styled <- l_style(original,
  color = "#194E70",
  font_size = "clamp(10pt, 2vmin, 18pt)", background = "white", padding = 4
)
l_measure(original)
l_measure(styled, list(width = 800, height = 600))
viewport <- l_style(l_viewport(), background = "#EDF3F5", padding = "2mm")
print(viewport)
}
\seealso{
\code{\link[=l_get_element]{l_get_element()}}, \code{\link[=l_text]{l_text()}}, \code{\link[=l_measure]{l_measure()}}, \code{\link[=l_register_element]{l_register_element()}}
}
