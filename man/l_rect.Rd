% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates.R
\name{l_rect}
\alias{l_rect}
\title{Create rectangles with native grid geometry}
\usage{
l_rect(
  x = grid::unit(0.5, "npc"),
  y = grid::unit(0.5, "npc"),
  width = grid::unit(1, "npc"),
  height = grid::unit(1, "npc"),
  just = "centre",
  hjust = NULL,
  vjust = NULL,
  default.units = "npc",
  name = NULL,
  gp = NULL,
  vp = NULL,
  ...
)
}
\arguments{
\item{x, y}{Numeric coordinates in \code{default.units}, or \code{\link[grid:unit]{grid::unit()}} objects.
Vectors are supported according to \code{\link[grid:grid.rect]{grid::rectGrob()}} recycling rules.}

\item{width, height}{Rectangle dimensions as numbers in \code{default.units} or
grid unit objects. These are internal geometry, not lplot layout lengths.}

\item{just}{Character or numeric justification relative to \verb{(x, y)}, such
as \code{"centre"} or \code{c("left", "bottom")}.}

\item{hjust, vjust}{Optional numeric horizontal and vertical justification,
overriding the corresponding component of \code{just}. Zero means left/bottom,
0.5 means center and one means right/top.}

\item{default.units}{Grid unit used for numeric geometry, by default \code{"npc"}.
Existing unit objects, including the defaults, retain their own units.}

\item{name}{Optional character name for the grid grob. This is distinct from
a scene node's \code{id} in \code{\link[=l_place]{l_place()}}.}

\item{gp}{\code{NULL}, a \code{\link[grid:gpar]{grid::gpar()}} object, or a uniquely named list of grid
graphical parameters. Unspecified child properties inherit from this group.}

\item{vp}{Optional grid viewport, viewport name or viewport path. A viewport
controls local coordinates, dimensions, justification, scales and clipping.}

\item{...}{Uniquely named \code{\link[grid:gpar]{grid::gpar()}} parameters overriding \code{gp}. Common
parameters are \code{fill}, \code{col}, \code{alpha}, \code{lwd}, \code{lty}, \code{lineend}, \code{linejoin},
\code{linemitre} and \code{lex}. Values are validated by grid, not by a CSS parser.}
}
\value{
A native grid \code{rect} grob, suitable for \code{\link[=l_template]{l_template()}} or direct
use with \code{\link[=l_place]{l_place()}} and \code{\link[grid:grid.draw]{grid::grid.draw()}}.
}
\description{
Construct one or more rectangles as a grob, using grid units and graphical
parameters. The default rectangle is centered and fills its current grid
viewport. Construction does not draw or open a graphics device.
}
\details{
Use \code{fill = NA} for an unfilled rectangle and \code{col = NA} for no outline.
Styles follow grid inheritance and device capabilities; an unspecified fill
does not guarantee a white background. The supplied \code{gp} is not modified.

Numbers default to normalized parent coordinates, with the origin at the
bottom-left. For example, \code{width = 0.5} occupies half the current viewport;
\code{width = grid::unit(20, "mm")} remains a physical 20 mm. Layout strings such
as \code{"50\%"} or \code{"8px"} belong to \code{\link[=l_place]{l_place()}}, not this constructor.
Using \code{"native"} coordinates requires appropriate grid viewport scales and
does not automatically connect the rectangle to a map's projection.

The grob has no padding, rounded corners or independent collision policy.
Use a containing scene node for layout properties. Several vectorized
rectangles remain one grob and one layout element.
}
\examples{
swatches <- l_rect(
  x = c(0.25, 0.75), width = 0.4, height = 0.7,
  fill = c("#95CEC0", "#194E70"), col = NA
)
scene <- l_viewport(list(
  l_place(swatches, left = 10, top = 10, width = 180, height = 60)
))
l_render(scene, width = 200, height = 80)
outline <- l_rect(
  width = grid::unit(20, "mm"),
  height = grid::unit(10, "mm"), fill = NA, col = "black", lty = "dashed"
)
grid::is.grob(outline)
}
\seealso{
\code{\link[=l_text]{l_text()}}, \code{\link[=l_template]{l_template()}}, \code{\link[grid:grid.rect]{grid::rectGrob()}}, \code{\link[grid:gpar]{grid::gpar()}}
}
