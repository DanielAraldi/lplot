% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/templates.R
\name{l_text}
\alias{l_text}
\title{Create text annotations with native grid typography}
\usage{
l_text(
  label,
  x = l_unit(0.5, "npc"),
  y = l_unit(0.5, "npc"),
  just = "centre",
  hjust = NULL,
  vjust = NULL,
  rot = 0,
  check.overlap = FALSE,
  default.units = "npc",
  name = NULL,
  gp = NULL,
  vp = NULL,
  ...
)
}
\arguments{
\item{label}{Text to display, including character vectors and mathematical
expressions such as \code{expression(alpha^2)}. Use \code{"\\n"} for explicit lines.}

\item{x, y}{Numeric coordinates in \code{default.units}, or \code{\link[=l_unit]{l_unit()}} / \code{\link[grid:unit]{grid::unit()}}
objects.
Vectors are supported according to \code{\link[grid:grid.rect]{grid::rectGrob()}} recycling rules.}

\item{just}{Character or numeric justification relative to \verb{(x, y)}, such
as \code{"centre"} or \code{c("left", "bottom")}.}

\item{hjust, vjust}{Optional numeric horizontal and vertical justification,
overriding the corresponding component of \code{just}. Zero means left/bottom,
0.5 means center and one means right/top.}

\item{rot}{Text rotation in degrees; for example, 90 gives vertical text.}

\item{check.overlap}{Logical; if \code{TRUE}, grid may omit overlapping labels
within this grob at draw time, in input order. It does not reposition labels
or detect collisions with other lplot nodes.}

\item{default.units}{Grid unit used for numeric geometry, by default \code{"npc"}.
Existing unit objects, including the defaults, retain their own units.}

\item{name}{Optional character name for the grid grob. This is distinct from
a scene node's \code{id} in \code{\link[=l_place]{l_place()}}.}

\item{gp}{\code{NULL}, a \code{\link[grid:gpar]{grid::gpar()}} object, or a uniquely named list of grid
graphical parameters. Unspecified child properties inherit from this group.}

\item{vp}{Optional grid viewport, viewport name or viewport path. A viewport
controls local coordinates, dimensions, justification, scales and clipping.}

\item{...}{Uniquely named \code{\link[grid:gpar]{grid::gpar()}} parameters overriding \code{gp}, including
\code{col}, \code{fontsize}, \code{fontfamily}, \code{fontface}, \code{lineheight}, \code{alpha} and \code{cex}.
Use grid names, not \code{\link[=l_style]{l_style()}} names such as \code{color} or \code{font_size}.}
}
\value{
A native grid \code{text} grob. \code{\link[=l_measure]{l_measure()}} can inspect its intrinsic
size, and \code{\link[=l_template]{l_template()}} can combine it with other graphical primitives.
}
\description{
Construct a text grob with native grid alignment, rotation, mathematical
expressions and vectorized labels. No drawing or device creation occurs
until the grob is rendered.
}
\details{
\code{fontsize} is numeric, in points. \code{fontface} accepts values such as \code{"plain"},
\code{"bold"}, \code{"italic"} and \code{"bold.italic"}. A \code{fontface} shortcut overrides a
\code{font} already stored in \code{gp}, and vice versa. Font availability and glyph
metrics depend on the output device. Graphical parameters are not mutated.

\code{lineheight} controls relative spacing for explicit line breaks. There is
no automatic line wrapping, Markdown or HTML interpretation. Assigning a
layout width does not turn a long label into a paragraph.

Internal coordinates use grid's bottom-left origin and default \code{npc} units.
Internal \code{just} and the outer \code{\link[=l_place]{l_place()}} \code{anchor} are independent. For a
fixed font size, place the grob directly. For responsive typography, wrap it
in \code{\link[=l_get_element]{l_get_element()}} and use \code{style = list(font_size = "clamp(...)")};
responsive length strings cannot be passed as grid \code{fontsize} values.
}
\examples{
note <- l_text("Source: survey\nLocal coordinates",
  x = 0, just = "left",
  gp = list(fontsize = 9, col = "#50666C"), lineheight = 1.3
)
l_measure(note)
labels <- l_text(c("West", "East"),
  x = c(0.2, 0.8),
  fontsize = 10, fontface = c("plain", "bold")
)
l_render(l_viewport(list(
  l_place(labels, left = 0, top = 0, width = 240, height = 60)
)), width = 240, height = 60)
formula <- l_text(expression(alpha^2 + beta^2), fontsize = 12)
grid::is.grob(formula)
}
\seealso{
\code{\link[=l_unit]{l_unit()}}, \code{\link[grid:grid.text]{grid::textGrob()}}, \code{\link[grid:gpar]{grid::gpar()}}, \code{\link[=l_get_element]{l_get_element()}}, \code{\link[=l_style]{l_style()}}
}
