% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lengths.R
\name{l_length}
\alias{l_length}
\title{Declare a device-independent layout length}
\usage{
l_length(value = "auto", unit = "px")
}
\arguments{
\item{value}{A single finite number, an existing \code{l_length}, or a string
containing a number and unit, \code{"auto"}, or a supported length expression.}

\item{unit}{Unit for numeric \code{value}: \code{"px"}, \code{"\%"}, \code{"pt"}, \code{"mm"}, \code{"cm"},
\code{"in"}, \code{"vw"}, \code{"vh"}, \code{"vmin"} or \code{"vmax"}. Ignored when \code{value} is
already a string or an \code{l_length}.}
}
\value{
An S3 \code{l_length} object containing an unresolved value or expression.
An existing \code{l_length} is returned unchanged.
}
\description{
Normalize a numeric value, a length string or an existing logical length.
The result stores a declaration rather than a device-dependent measurement;
it is evaluated in context by the layout resolver at render time.
}
\details{
One logical pixel is 1/96 inch and one point is 1/72 inch. Device DPI affects
raster density, not the definition of these lengths. Percentages use the
containing content box on the relevant axis. \code{vw} and \code{vh} are percentages
of the root width and height; \code{vmin} and \code{vmax} use their minimum and maximum.

Strings accept nested \code{min()}, \code{max()} and \code{clamp()} expressions. Parsing
does not evaluate R code. \code{auto} is not allowed inside an expression. A
character value such as \code{"24"} needs a unit; the numeric value \code{24} uses
\code{unit}. Negative values can describe offsets, but negative dimensions are
rejected by the node or resolver where they are used.

\code{auto} is context-dependent: it uses intrinsic content size for ordinary
elements and available space for plots, panels and viewports. It is not a
request for text wrapping. These values are not \code{\link[grid:unit]{grid::unit()}} objects.
}
\examples{
l_length("50\%")
l_length(2, "cm")
l_length("min(40mm, max(10px, 25\%))")
scene <- l_viewport(list(
  l_place(l_rect(fill = "grey80"), width = l_length("50\%"), height = 30)
))
l_resolve(scene, width = 400, height = 100)$root$children[[1]]$box
}
\seealso{
\code{\link[=l_clamp]{l_clamp()}}, \code{\link[=l_place]{l_place()}}, \code{\link[=l_resolve]{l_resolve()}}
}
